# V3.0 未完成工作清单（审计结果）

> 审计日期：2026-09-12
> 审计依据：`docs/V3.0开发规格说明书.md`
> 审计方法：代码实测核查（非文档推测）。规格书 84 项模块级验收未勾选项逐一对照后端/前端代码验证。
> 审计结论：84 项未勾选验收项中，约 70 项功能已真实实现（仅缺勾选记录），14 项存在实质缺口（5 项完全缺失、9 项部分实现/断链）。

---

## 一、真正缺失（需开发）

### 1.1 顿悟系统（I-11.9）奖励结算断链 —— 高优先级

规格书 11.9.9 验收标准（L1709-1719）共 11 项，其中 **5 项实质缺失**：

| # | 验收项 | 状态 | 证据（文件:行号） |
|---|--------|------|------------------|
| 5 | 顿悟奖励 +100 XP，概率掉落史诗/传说卡片 | ✅ 已修复（2026-09-12） | `backend/agent/chat.py:346-354` 顿悟分支 `add_xp(pet,100)` + `drop_card(source="insight", force_rarity="epic"/"legendary")`；SSE `insight_event` 已补 `card_dropped` 字段（L635-648），E2E 实测 xp=100 落账 |
| 8 | 主动挑战机制：邀请话术 + 挑战题库 + 挑战成功大额奖励 | ✅ 已修复（2026-09-12） | `chat.py:228-289` `_settle_challenge_success` + `challenge_success` SSE + challenge_completed 计数；persona 邀请话术；E2E 实测 title 打折陷阱、xp_reward=25、掉落稀有卡、解锁⚔️挑战者徽章 |
| 10 | 延迟顿悟：非学习时间说"昨天那道题我想通了"触发奖励 | ✅ 已修复（2026-09-12） | `chat.py:315-354` `is_delayed_insight` 优先接入（L320），solved=True、thinking_problems_solved+1、+100XP、史诗/传说卡、🌙延迟顿悟徽章；E2E 实测 |
| 11 | 顿悟徽章系统完整：8 种徽章，解锁条件正确 | ✅ 已修复（2026-09-12） | `student.py:311-318` 8 种徽章常量 + `student.py:339-346` 解锁条件表；`assessment.py` `check_insight_badges` 阈值判定（10/50/100 次、挑战、思考题）；E2E 实测 ⚔️挑战者达标 |
| 9 | 每日思考题：会话结束留题，第二天开场询问 | ✅ 修复完成（2026-09-12） | `chat.py:872-884` end_session 存题并写 `summary["thinking_problem"]`；`start_session`（L95）次日 <今日日期 才追问；前端 farewell 弹窗展示；TP_CHECK 实测 |

**顿悟系统整体状态**：检测器（`insight_detector.py`）与数据模型（`student.py:190-196`）已就绪；断点在于"奖励结算（XP/卡片/徽章）"、"挑战闭环"、"思考题次日闭环"、"延迟顿悟调用"四类均未接线。11 项验收中 2 项完整（#1 感叹词识别、#2 关键步骤识别）、4 项部分（#3 庆祝流程、#4 特效、#6 日志、#7 线索）、5 项缺失（上表）。

**其他顿悟相关发现**：
- SSE 事件名不符规格：规格要求 `insight_event`（L1591），代码实际发送 `"type": "insight"`（`chat.py:444`），且缺 `card_dropped`/`badge_unlocked` 字段 → ✅ 已修复（2026-09-12）：SSE 已改发 `"type": "insight_event"`，载荷含 `insight_type`/`student_quote`/`xp_reward`/`card_dropped`/`badge_unlocked`（`chat.py:648-660`），E2E 实测事件类型正确。
- 无 API 暴露：`main.py` 全部 54 个端点无 insight/challenge/thinking-problem 端点 → ✅ 已解决（2026-09-12）：顿悟/挑战经聊天 SSE 内联暴露（insight_event/challenge_success），思考题经 `/api/session/end` 返回 `summary.thinking_problem` + `/api/session/start` 次日询问，无需独立 REST 端点，与规格 11.9 集成点设计一致。
- 无测试覆盖：`tests/` 无任何顿悟相关测试 → ✅ 已补（2026-09-12）：E2E 脚本落地 `temp/opencode/`（e2e_challenge.py / delayed_check.py / tp_check.py / todo7_smoke.py），覆盖挑战成功结算、延迟顿悟、思考题次日闭环、出题入库，全部通过。

### 1.2 人性设计原则（I 模块）—— 中优先级

| 子项 | 状态 | 证据 |
|------|------|------|
| I-11.5 奇怪的现象（随机插入口算技巧） | ✅ 已修复（2026-09-16） | `persona.py:683-711` 新增 `STRANGE_PHENOMENA` 10 条 + `pick_strange_phenomenon()`；`chat.py` 打字输出后 8% 概率插入 ✨课文案（每会话上限 2 次，`strange_inserted` 计数随 start_session 重置）；纯学习模式/欢迎态不触发 |
| I-11.7 连续剧情铺垫（"昨天学会的技巧今天派上用场"） | ✅ 已修复（2026-09-16） | `chat.py:117-133` 新增 `_yesterday_prelude()`：取 session_history 最后一条，1~3 天前且含话题名 → 开场注入铺垫话术（start_session L65，顺序：钩子→铺垫→心情开场） |
| I-11.7 剧情选择（PBL 关键节点分支） | ✅ 已修复（2026-09-16） | `projects.py` missile_trajectory 新增 `choices`（lv2 通关后首个选择点）；`pbl_service.py` `get_pending_choice`/`answer_choice` + `choices_answered` 记录；`main.py` 新增 GET `/api/pbl/projects/{id}/pending-choice` + POST `.../choice`；`pbl.js` 选择弹窗 + `next_intro` 引导话术；冒烟实测 lv2 通关→返回选择点→作答→pending=None |

### 1.3 Combo 系统两个缺陷（模块 C，验收 5.8）—— 低优先级

| 缺陷 | 状态 | 证据 |
|------|------|------|
| 答错鼓励话术缺失 | ✅ 已修复（2026-09-16） | `assessment.py:307-314` 新增 `_COMBO_RESET_ENCOURAGEMENT` 6 条；归零分支 `message` 改为 `_random.choice(...)`（L484）；`combo.js` 新增 `showComboResetEncouragement()` 淡入淡出 toast + `combo.css` 样式；SSE `current===0 && message` 触发 |
| "对/嗯"不触发 Combo 无确定性过滤 | ✅ 已修复（2026-09-16） | `chat.py:57-69` 新增 `_MINOR_CONFIRM_WORDS` 白名单 + `_is_minor_confirmation()`（长度≤4 精确匹配）；`apply_eval(..., suppress_combo=...)` 传递；`_apply_p1_rewards` combo 分支包裹 `not suppress_combo`（newly_mastered 掉落与宠物 XP 仍执行） |
| 附带 Bug：`previous` 恒为 0 | ✅ 已修复（此前） | `assessment.py:409` prev 先取值再 increment（P1 时代修复，确认无回归） |

---

## 二、部分实现（断链需修补，改动量小）

| 项 | 断链点 | 验收对应 |
|----|--------|---------|
| I-11.3 孩子出题 | ✅ 已修复（2026-09-12）：聊天页"＋"菜单新增"出一道题"入口（`index.html` plus-menu `btn-problem`）+ 出题弹窗（题目/参考答案/判分按钮），提交 `POST /api/problems/{student_id}/create`（`app.js` openProblemModal/submitProblem），出题 +10XP + 概率掉卡；冒烟实测 200 且掉卡返回 | I-11.3 |
| I-11.3 作品展示墙 | ✅ 已修复（2026-09-12）：首页导航新增"🎨 我的作品"Tab（`index.html` data-view=creations）+ `view-creations` 容器 + `app.js` renderCreations 渲染 problem/teach 两类作品（判分结果/质量/日期，空态文案），切换视图即拉取 `GET /api/creations`；冒烟实测 200 | I-11.3 |
| I-11.5 结尾悬念 + I-11.7 故事线 | ✅ 已修复（2026-09-12）：`app.js:1486-1495` 接收并展示 `/api/session/end` 响应的 closing_hook/fun_fact/story_reveal/thinking_problem（告别弹窗），孩子退出前可见 | I-11.5 / I-11.7 |
| 3.7 宠物随机互动语音 | ✅ 已修复（2026-09-16）：`pet.js:253` petInteract 保留 toast + `pet.js:262` playPetInteract() 按返回 animation 给头像加动画类（500ms 移除）；`app.js:440-446` 点击头像触发语音动画并开面板；`style.css:333-374` 4 个 keyframes（happy_jump/sparkle/spin/wooble）；冒烟实测返回 `animation` 字段 | 3.7 L268 |
| 3.7 升级庆祝动画 | ✅ 已修复（2026-09-16）：`pet.js:55-90` handlePetFeedSSE 对比新旧等级/新解锁皮肤，升级时 `pet.js:93-116` showPetLevelUpModal() 弹窗（宠物 emoji + 🎉 + Lv.X→Lv.Y + 新皮肤 + 彩带），复用庆祝特效 | 3.7 L269 |
| 4.7 卡片掉落特效 | ✅ 已修复（2026-09-16）：`cards.js:135-158` showCardReveal 改双面结构，600ms 后 `big.classList.add("flipped")` 触发 rotateY 翻牌；`cards.css` perspective:900px + preserve-3d + backface-visibility:hidden，背面斜纹＋「?」＋🎴 | 4.7 L453 |
| 4.7 星光兑换安全隐患 | ✅ 已修复（2026-09-16）：`cards.py:258` 新增 `SKIN_PRICES` 价目表（sakura=100/starry=200/golden=300/cosmic=500）；`exchange_skin(cards, skin_id, cost=None)` 白名单校验 + 忽略客户端 cost；`main.py` CardExchangeRequest.cost 改可选、端点不传客户端 cost；冒烟实测 cost=1 仍扣 100 | 4.7 L454 |
| 6.7/7.7/8.8/10.5/J.10 模块验收 | 功能已实现，规格书未勾选（见第三节） | 各模块验收区 |
| 11.4 身份认同展示位 | ✅ 已修复（2026-09-16）：`identity.py:65-172` 四个标签触发条件对齐规格 L1500（神射手=导弹通关/建筑大师=PBL≥6关或冒险≥6关/建模侦探=real_world_problems_solved≥3/出题大师=creations type=="problem"≥10，缺字段安全退化）；`index.html:106` 状态栏 identity-chip + `app.js:1005-1023` 拉取渲染 + `style.css` chip 样式 + translations 双语 | I-11.4 |

---

## 三、已实现但规格书未勾选（84 项中约 70 项，只需补勾验证记录）

代码核查确认以下模块功能已真实实现，仅规格书验收清单从未勾选：

| 模块 | 验收区 | 实现证据 |
|------|--------|---------|
| A 宠物 | 3.7（L262-274，8 项） | `pet.py` SKINS 5/10/15/20 级解锁（L32-38）；mood 计算（L73-96）；add_xp 升级（L122-156）；`main.py` L794-823 API；`pet.js` 皮肤 UI 完整 |
| B 卡片 | 4.7（L447-458，8 项） | `cards.py` build_library ≥78 张×4 稀有度（L48+）；掉落概率（L23-24）；`cards.js` 收藏墙/翻牌/星光兑换 |
| D 冒险地图 | 6.7（L761-773，9 项） | `adventure_levels.py` 78 normal+34 boss=112 关，`validate()` 通过；与星空图同源 syllabus（实测 78 节点一致）；`main.py` L1068-1137 |
| E PBL | 7.7（L968-980，9 项） | `projects.py` 多项目/推荐/徽章（L189）；`pbl_service.py` enter/complete；`main.py` L1179-1245 四点端点（此前 E2E 验证） |
| F 导弹 | 8.8（L1116-1130，11 项） | `simulators/missile.js` 风/移动靶/编辑器（L24-31、42-47、204+）；Lv.1-6 此前 E2E 全通过（含 Lv.6 编辑器） |
| H 前10分钟 | 10.5（L1420-1432，9 项） | `chat.py` lightweight_mode（L49、294-304）；兴趣开场问题（L51-56）；不想学处理（L306-317） |
| J 多 LLM | J.10（L1973-1988，12 项） | `config.py` 5 提供商（L31-63）；`llm.py` 429 退避（L82-93）、降级+SymPy 校验（L118-127）；前端 provider 选择/测试连接（`index.html` L201-218）；旧配置兼容（`config.py:160-168`） |

> ✅ **回退项已解除（2026-09-12）**：上一轮审计指出 13.2 整体验收中"所有奖励（XP/卡片/宠物成长）只在真实学习成功后触发"因顿悟 XP 未实际入账而证据不足。现顿悟奖励已修复并经 E2E 实测：延迟顿悟 `xp=100` 真实落账（delayed_check），挑战成功 `xp_reward=25` + 稀有卡（e2e_challenge），出题 `+10 XP` + 掉卡（todo7_smoke）——奖励均在真实学习事件后触发，13.2 该项**保持勾选**，无回退。

---

## 四、实施计划（按收益/成本排序）

| 序 | 任务 | 模块 | 预估改动 | 验收解锁 | 状态 |
|----|------|------|---------|---------|------|
| 1 | `app.js:1330` 接收并展示 session/end 的 closing_hook/fun_fact/story_reveal | I-11.5/11.7 | 前端 1 处 | I-11.5 悬念/秘密、I-11.7 故事线 | ✅ 完成（告别弹窗 L1486-1495） |
| 2 | chat.py 顿悟分支补 add_xp(100) + drop_card(史诗/传说) + SSE 补 card_dropped/badge_unlocked 字段 | I-11.9 | 后端 1 处 | 验收 #3/#5 | ✅ 完成（L346-354、L635-648） |
| 3 | student.py 8 种顿悟徽章常量 + assessment.py 阈值判定（10/50/100 次、挑战、思考题） | I-11.9 | 后端 2 处 | 验收 #11 | ✅ 完成（student.py L311-318、L339-346） |
| 4 | 挑战闭环：邀请话术（persona）+ challenge_completed 计数 + 成功奖励（3-5 倍 XP、必掉稀有卡） | I-11.9 | 后端 2 处 | 验收 #8 | ✅ 完成（chat.py L228-289） |
| 5 | 每日思考题闭环：结束展示给前端 + start_session 次日询问 + is_delayed_insight 接入 + thinking_problems_solved 计数 | I-11.9 | 后端 2 处 + 前端 1 处 | 验收 #9/#10 | ✅ 完成（end_session L872-884、start_session L95、delayed L315-354） |
| 6 | 前端出题 UI + 作品墙 Tab（复用现成 API） | I-11.3 | 前端 2 处 | I-11.3 | ✅ 完成（index.html + app.js，冒烟 200） |
| 7 | 补勾剩余验收项验证记录 + 回退 13.2 证据不足单项 | 全部 | 文档 | 13.2 | ✅ 完成（本次回填；13.2 证据已补齐不回退） |
| 8 | Combo 鼓励话术 + 星光兑换服务端校验 + 人性化设计（奇怪现象/剧情铺垫/剧情选择）+ 前端断链（宠物语音/升级弹窗/翻牌/身份展示） | C/B/I | 后端 7 处 + 前端 9 处 | 5.8、4.7、I-11.5、I-11.7、3.7、I-11.4 | ✅ 完成（2026-09-16；顺带修复"对/嗯"刷分过滤，详见一、二节 + 冒烟实测） |

---

## 五、风险与备注

- **服务器**：实施期间后端改动需重启服务生效（当前 PID 3068，`http://127.0.0.1:8000`，全网监听确认）。
- **前端缓存**：修改前端 JS 后必须 bump `?v=` 版本号并带 query 刷新。
- **验收纪律**：每个修复完成后需对应勾选规格书验收项，避免出现"实现了但无记录"。
- **文档同步**：规格书 9.8/13.2/13.3/13.4 已勾选的项，若实施中涉及需同步更新。